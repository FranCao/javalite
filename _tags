# Include the llvm and llvm.analysis packages while compiling
true: package(llvm), package(llvm.analysis)

# Enable almost all compiler warnings
true : warn(+a-4)

# Instruct ocamlbuild to ignore the "reversestring.o" file when it's building
"reversestring.o": not_hygienic

# Instruct ocamlbuild to ignore the "stringupper.o" file when it's building
"stringupper.o": not_hygienic

# Instruct ocamlbuild to ignore the "stringlower.o" file when it's building
"stringlower.o": not_hygienic